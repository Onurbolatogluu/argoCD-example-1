apiVersion: v1
kind: Pod
metadata:
  name: flask-app-pod
  labels:
    app: flask-app
spec:
  containers:
    - name: flask-app
      image: onurbolatogluu/flask-web-counter:latest
      ports:
        - containerPort: 5000
